import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: {
    default: "PickSmart Magazine — Build Smarter. Create Better.",
    template: "%s | PickSmart Magazine",
  },
  description:
    "PickSmart Magazine is a trusted woodworking and DIY resource covering tools, projects, buying guides, and honest reviews to help you build smarter.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full antialiased">
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
